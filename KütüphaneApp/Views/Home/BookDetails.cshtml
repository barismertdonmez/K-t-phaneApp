@model Data.DataModels.DataBookDetailsModel

<link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">

<div class="container">
    <div class="card">
        <div class="card-body">
            <h3 class="card-title">@Model.Book.BookName</h3>
            <h6 class="card-subtitle text-sm-start">@Model.Book.WriterName</h6>
            <hr />
            <h6 class="card-subtitle text-sm-start">
                @foreach (var item in Model.Categories)
                {
                   <p>@item.CategoryName</p>
                }
            </h6>
            <div class="row">
                <div class="col-lg-4 col-md-4 col-sm-12 my-2">
                    <div class="white-box text-center img-fluid"><img src="~/images/@Model.Book.ImageUrl" class="img-responsive" style="margin-top:40px;"></div>
                </div>
                <div class="col-lg-8 col-md-8 col-sm-12">
                    <h4 class="box-title mt-5">Book description</h4>
                    <p>Fazla çalıştırılan ve kötü muamele gören hayvanlar bir gün toplanıp yaşadıkları 
                        çiftliği ele geçirirler. Sonunda söz sahibi olmuşlardır, çiftlikte daha adil ve eşit 
                        bir toplum oluşturmaya kararlıdırlar. Domuzların öncülüğünde bu yeni düzeni kurmak için çalışmaya başlarlar.
                        Bu düzen ilk başta çiftliğin gelişmesini sağlasa da zamanla hayvanların 
                        öngöremediği sorunlar ortaya çıkacak ve eskisinden daha acımasız bir rejim kurulacaktır.</p>
                    <form asp-controller="Cart" asp-action="AddToCart" method="post" class="form-inline">
                        <input type="hidden" name="bookId" value="@Model.Book.Id" />
                        <div class="input-group mb-3">
                            <input type="number" name="quantity" value="1" min="1" step="1" class="form-control" style="width:100px" />
                            <div class="input-group-append">
                                <button class="btn btn-dark btn-rounded mr-1" data-toggle="tooltip" title="" data-original-title="Add to cart">
                                    <i class="fa fa-shopping-cart">Add To Cart</i>
                                </button>
                            </div>
                        </div>
                    </form>
                    <h3 class="box-title mt-5">Key Highlights</h3>
                    <ul class="list-unstyled">
                        <li><i class="fa fa-check text-success"></i>IsRented</li>
                        <li><i class="fa fa-check text-success"></i>IsApproved</li>
                    </ul>
                </div>
                <div class="col-lg-12 col-md-12 col-sm-12">
                    <h3 class="box-title mt-5">General Info</h3>
                    <div class="table-responsive">
                        <table class="table table-striped table-product">
                            <tbody>
                                <tr>
                                    <td width="390">Book Name</td>
                                    <td>@Model.Book.BookName</td>
                                </tr>
                                <tr>
                                    <td>Writen Name</td>
                                    <td>@Model.Book.WriterName</td>
                                </tr>
                                <tr>
                                    <td>Total Pages</td>
                                    <td>@Model.Book.TotalPage</td>
                                </tr>
                                <tr>
                                    <td>Categories</td>
                                    @foreach (var item in Model.Categories)
                                    {
                                        <td>@item.CategoryName</td>
                                    }
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<div>
    @foreach (var item in Model.Categories)
    {
        @item.CategoryName;
    }
</div>